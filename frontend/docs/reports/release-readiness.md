# Готовность объединённого релиза — 23.09.2026

## Проверенный код

Код приложения **`7073164`**: новый API и UI альтернатив Тамерлана, сравнение попыток, защита пустого HTTP-ответа, чистые снимки 3D и исправления клавиатурного фокуса. Коммит **`9068f1c`** добавляет финальные отчёты Тамерлана, не меняя приложение.

| Проверка | Результат и версия |
| --- | --- |
| Frontend lint / build / typecheck | Успешно на коде `7073164`, независимый прогон интегратора |
| Frontend unit | 74 / 74 |
| Backend C# / HTTP | 45 / 45 и 45 / 45; 400 наборов, 120 перестановок; Docker-аудит интегратора |
| Чистый Docker Compose интегратора | `6929bf4`, Brev: оба контейнера healthy; до последнего UI альтернатив |
| Чистый клон и браузер Тамерлана | `88a0c8c`, mock без ключа: 110 / 110; полный цикл альтернатив и сравнения |
| Последующая проверка Тамерлана | `7c0570e`: 31 / 31 (пустые HTTP 503, повтор, мобильный обзор); `7073164`: повторная production-сборка Docker |
| Контрольный набор | 52.56 → 56.54, spent 95, remaining 5 |
| Применение альтернативы | Без автоматической оценки; после явного нажатия 57.21 / 100, сравнение +0.67 |
| Реальный AI у Тамерлана | Один запрос, HTTP 200, recommendation.source=llm; проверенные вариант и аргументы |
| Клавиатура Claude 2 | `7073164`, 390 px, 32 trusted события: оба дефекта фокуса исправлены; 0 POST |
| Визуальная проверка альтернатив Claude 1 | `7073164`, 9 сочетаний языков/тем/ширин; без переполнения. Это выборка, не все 18 сочетаний |
| Публичный 3D | `631efbb`, Firefox 156 / Intel Arc: живая сцена, выбор районов, возврат с решений |

Источники: [независимая проверка](architect-audit.md), [финальная интеграция Тамерлана](../../../docs/reports/final-integration.md), [Claude 1](claude-1.md), [Claude 2](claude-2.md), [приёмка отчётов](report-review.md).

## Публичная версия

SSH-сверка интегратора около **17:22 (+05:00)**: frontend **`akim-frontend:631efbb`**, API **`akim-5h-api:release-41812ef`**. Все три контейнера healthy, но Swagger через gateway ещё **не содержит `/api/simulations/alternatives`**. Состояние healthy не подтверждает актуальность кода.

Frontend запущен из `/home/frontend/releases/akim-631efbb`; API — из `/home/ubuntu/workspace/hack-4c816c70-team`. Диагностика GPT независимо выявила старый API-образ. Дополнительная проверка интегратора: серверный `.env` принадлежит `ubuntu:ubuntu`, права `600`, пользователь `frontend` его не читает; `sudo -n` недоступен. Поэтому рецепт копирования этого `.env` под frontend без участия владельца не является воспроизводимым.

По передаче Тамерлана сервер обновляет DevOps с доступом владельца `ubuntu`. Сохранить действующий приватный `.env`, собрать **оба** сервиса из согласованного чистого checkout и с обоими compose-файлами. Не выводить секреты в отчёт. Команды — в README и отчёте Тамерлана.

В этом проходе интегратор не выполнял деплой. Push в GitHub сам по себе не обновляет сайт.

## Осталось перед финальной публичной приёмкой

- Опубликовать согласованную версию API и frontend, проверить SHA образов и наличие альтернатив в Swagger.
- На публичном сайте повторить контрольный план, поиск альтернативы, перенос choices без автоматического POST, явную оценку и сравнение; 390 px и RU/KK/EN.
- Небольшой UX-дефект из нового отчёта Claude 1: чипы выбора цели имеют высоту 40 px вместо проектных 44 px. Основной цикл не блокирует; исправление ещё не включено.
- Физические мобильные устройства, другие GPU и скринридер не проверены. Публичная сцена проверена на Intel Arc, а мобильные сценарии — в эмуляции.

Основной цикл подтверждён на тестовых стендах. Финальная публичная приёмка последнего релиза ещё не завершена.
