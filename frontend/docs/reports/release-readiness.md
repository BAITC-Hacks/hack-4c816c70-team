# Release readiness — frontend

## Кандидат на публикацию

- **Git SHA:** `29817a7bc50d455e1aa21ecded9076b2d47b6f78` (`origin/main`, Hizeshi).
- **Статус:** готов к финальной публикации после решения владельца деплоя. В этом проходе деплой не выполнялся.
- **Состав:** локализация RU/KK/EN, BCP 47 `Accept-Language`/`Content-Language` для explanation, защита двойной оценки и stale-ответов, сохранение черновика, честный пустой `/results`, 3D/городская визуализация, локализованные API-ошибки.

## Проверки кандидата (локальная общая копия)

| Проверка | Результат | Основание |
| --- | --- | --- |
| `npm run lint` | успешно | последний прогон GPT перед `29817a7` |
| `npm test` | 55/55 | последний прогон GPT перед `29817a7` |
| `npm run build` | успешно | `/`, `/decisions`, `/results` сгенерированы |
| Локальный браузер, контрольный набор | пройден | Claude 2: `95 / 5 / 52.56 → 56.54` на RU и KK; EN после явного обновления explanation |
| Смена UI-языка готового результата | пройдена | Claude 2: нет автоматического POST, старый explanation помечен фактическим языком |
| Явное обновление explanation | пройдено | Claude 2: один POST, прежний результат сохраняется при сетевой/502 ошибке |
| Изменение плана, reset, прямой `/results` | пройдены | Claude 2: результат сбрасывается только после изменения/сброса, `/results` без результата честно пуст |
| 390 px и 1440 px | пройдено | Claude 2: горизонтального overflow не найдено |
| Локализация unknown HTTP/contract error | unit-проверена | `29817a7`: русская `detail` не попадает в EN/KK основной текст; форматирование зависит от текущего locale |

## Что не является публичной проверкой

Сверка интегратора 23.09.2026 около 16:56 (+05:00): публичный frontend работает из **`631efbb`**. SSH-проверка подтвердила образ `akim-frontend:631efbb` и label `org.opencontainers.image.revision=631efbb`; frontend, API и gateway — `healthy`. Обновлялся только frontend; этот SHA не обозначает версию запущенного API. `deployment.md` описывает исторический релиз `f3f1981`, а не текущий сервер.

Claude 1 проверил публичную сцену на `631efbb`: три темы, переходы между страницами, изменение ширины 390 → 1440 и fallback при отключённом WebGL. Исправление error detail из `29817a7` ещё не опубликовано на сервере. Проверки Claude 2 выполнены локально с API через туннель — не считать их полным публичным E2E.

Перед публичной публикацией владельцу деплоя следует развернуть именно `29817a7` и повторить health, routes, favicon и контрольный POST/UI. Деплой, Compose и серверная конфигурация в этом проходе не менялись.

## Незавершённые пункты и ограничения

- Публичный стенд не обновлён до текущего SHA.
- Чистый запуск финального кандидата именно в WSL Ubuntu по обновлённому корневому README ещё не повторён. Документационные команды сверены с compose-файлами, локальные ссылки проверены; это не заменяет финальный clean-install прогон.
- В браузере на `29817a7` не воспроизводились unknown HTTP и contract errors на KK/EN; покрытие для них — unit/regression test и code review Claude 2. Настоящие network timeout/обрыв сети также не запускались, использовался controlled `fetch` failure.
- В скрытой вкладке Claude 2 не мог проверить реальные физические клавиши; действия кнопок и смена языка выполнялись через реальные DOM controls.
- Сценарий использует серверный `mock` или `llm` по состоянию API. Интерфейс различает «Шаблонное объяснение» и «AI-анализ» и не выдаёт mock за AI.
- Полоска текста в fallback-снимках исправлена Claude 1 новыми снимками canvas; интегратор просмотрел оба PNG. Исправление включается в следующий релиз, на `631efbb` ещё старые изображения.
- Интерактивный WebGL в Firefox и на физических мобильных устройствах не подтверждён. Firefox-снимок показывает модель, но сделан до окончания загрузки.
- Отчёты/скриншоты включаются отдельным документационным коммитом после SHA кандидата; код приложения этим не меняется.

## Короткий итог для публикации

Публиковать **`29817a7bc50d455e1aa21ecded9076b2d47b6f78`** или последующий документационный коммит с тем же кодом как frontend-кандидат. Локальные lint, build и 55/55 тестов успешны; контрольный набор подтверждён. После деплоя нужна короткая публичная проверка на этом SHA: текущий frontend — `631efbb`.
