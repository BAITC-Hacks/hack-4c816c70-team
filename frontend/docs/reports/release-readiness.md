# Готовность объединённого релиза — 23.09.2026

## Проверенный код

**`15f79b7`** объединяет новый API и UI альтернатив Тамерлана, сравнение попыток, исправление пустого HTTP-ответа и чистые снимки 3D от Claude 1. Последующий документационный коммит обновляет README и отчёты, не меняя приложение.

| Проверка | Результат и версия |
| --- | --- |
| Frontend lint / build / typecheck | Успешно, `15f79b7`, независимый прогон интегратора |
| Frontend unit | 74 / 74, `15f79b7` |
| C# regression | 45 / 45, backend в `6929bf4` — такой же, как в `15f79b7` |
| HTTP regression | 45 / 45; 400 наборов, 120 перестановок, новые альтернативы и языки |
| Чистый Docker Compose | `6929bf4`, Brev: оба контейнера healthy, страницы/favicon/PNG — 200 |
| Контрольный набор | 52.56 → 56.54, spent 95, remaining 5 |
| Публичный браузерный сценарий | Claude 2 на `631efbb`: контрольный план, RU/KK/EN, несовместимости, 390/1440 |
| Публичный 3D | Claude 1 на `631efbb`: темы, навигация, изменение ширины, fallback при отключённом WebGL |

Подробности: [независимая проверка](architect-audit.md), [Claude 1](claude-1.md), [Claude 2](claude-2.md), [ограничения локального clean-start GPT](clean-start.md).

## Публичная версия

Повторная независимая SSH-сверка после 17:11 (+05:00): frontend **`akim-frontend:631efbb`**; frontend, API и gateway healthy. Каталог frontend-релиза: `/home/frontend/releases/akim-631efbb`. Это версия frontend, не идентификатор запущенного API. Публичный Swagger через gateway ещё **не содержит `/api/simulations/alternatives`**. Исторический `deployment.md` с `f3f1981` не описывает текущий источник.

В этом аудите демо не обновлялось. Наличие изменений в GitHub само по себе не подтверждает их публикацию на сайте. Для нового UI альтернатив нужен актуальный API с `/api/simulations/alternatives`.

## Осталось перед финальной приёмкой

- Собрать и опубликовать согласованный SHA с новым frontend. Контейнерная проверка `6929bf4` не включает UI альтернатив из `88a0c8c`; его локальная сборка и тесты прошли отдельно.
- После деплоя проверить основной план, поиск альтернативы, перенос choices без автоматического POST, явную оценку и сравнение попыток; отдельно 390 px и RU/KK/EN.
- Проверка именно WSL Ubuntu не выполнена: на Windows у GPT нет Docker и WSL-дистрибутива. Успешный Docker-прогон интегратора выполнен на Brev.
- Claude 1 подтвердил интерактивную сцену в обычном Firefox 156 на Intel Arc; мобильные устройства и другие GPU не проверены. Claude 2 выполнил trusted keyboard events и нашёл два дефекта фокуса; интегратор исправил и проверил их локально. Детали — в [приёмке отчётов](report-review.md).
- Полоска подписи на fallback-снимках исправлена в новых PNG и просмотрена интегратором; на `631efbb` ещё старые изображения.

Кодовых блокеров в проверенной области не обнаружено. Статус не означает завершённую публичную приёмку последнего релиза.
