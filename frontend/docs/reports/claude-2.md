# Отчёт Claude 2 — конструктор пяти решений

## Исполнитель и commit

Claude 2. Commit не создавался: по договорённости git ведёт GPT. На момент сдачи HEAD общего каталога — `4e71c95`. Все файлы ниже не отслеживаются git (untracked). Pull, commit, push и переключение веток не выполнялись.

## Изменённые файлы

Только `features/planner/` и этот отчёт:

```text
features/planner/index.ts                 # публичные exports
features/planner/selection-rules.ts       # validateDraft + доступность действий + иммутабельные операции над черновиком
features/planner/selection-rules.test.ts  # 28 тестов пограничных правил (node:test)
features/planner/labels.ts                # подписи направлений, форматирование ru-RU только для вывода
features/planner/Planner.tsx              # "use client", композиция, локальное состояние формы
features/planner/MeasureCard.tsx          # карточка меры
features/planner/DistrictPicker.tsx       # select района с причинами недоступности
features/planner/DecisionTray.tsx         # пять слотов, бюджет, сводка причин, ошибка сервера, оценка
features/planner/planner.module.css
docs/reports/claude-2.md
```

`app/`, `package.json`, `lib/contracts/ui.ts` и чужие модули не менялись.

## Exports

```ts
import { Planner, validateDraft } from "@/features/planner";
```

- `Planner(props: PlannerProps)` — строго по `lib/contracts/ui.ts`.
- `validateDraft: ValidateDraft` — чистая функция, `(scenario, choices) => DraftValidation`.

Изменений публичного контракта нет. `Simulator.tsx` у GPT уже использует оба export с этими props.

## Что работает

**validateDraft.** Проверяет только ограничения выбора, все правила берёт из `scenario.rules`:
- `rules === null` → единственная ошибка `rules-unavailable`, `canSubmit: false`;
- `choice-count`: ровно `requiredChoices`;
- `duplicate-measure`: повтор меры отклоняется и в разных районах;
- `unknown-measure`;
- `district-required`: `undefined`, `""` и `null` считаются отсутствием района;
- `invalid-district`: чужой ID района, без попыток угадать;
- `district-for-city`: у городской меры есть любое значение `districtId`;
- `budget-exceeded`: строго `spent > budget`, ровно 100 допустимо, цены не округляются;
- `category-limit`: больше `maxPerCategory` уникальных мер одного направления;
- `incompatible`: `global` — пара в любых районах; `same-district` — только при совпадении `districtId`, для каждого района своя ошибка.

`provisionalSpent` и `provisionalRemaining` — это только сумма цен каталога для формы.

**Planner:**
- Каталог из `scenario.measures`, фильтр «Все» + 5 направлений с счётчиком `N/2`. Переключение фильтра не трогает choices.
- На карточке: направление и ID, название, охват «Район / Весь город», цена, лаг, эффекты с подписью «Полный эффект из каталога, без учёта лага». Отрицательный эффект (T1 у M11) выделен знаком ▼ и цветом.
- Select района есть только у районной меры, районы берутся из `scenario.districts`. `preferredDistrictId` подставляется в новые карточки, если не конфликтует, и об этом есть видимая строка. Назначение редактируется в карточке и в слоте.
- Городская мера создаётся как `{ measureId }`: ключа `districtId` нет, в JSON его тоже нет. Сброс района у районной меры тоже удаляет ключ.
- Районную меру можно добавить без района: слот помечается «Район не выбран», оценка блокируется.
- M1/M3 блокирует всю карточку с причиной «Нельзя вместе с M…». M4/M7 и M5/M13 делают недоступным только конкретный район в select, с текстом «Недоступно — Нура: уже выбрана M4. …». Остальные районы и карточка остаются доступными. При смене района или удалении меры всё пересчитывается сразу.
- Причины блокировки показаны текстом под действием, без tooltip: заполнено 5 из 5, лимит направления, глобальная несовместимость, «Недостаточно бюджета: не хватает N ед.», конфликт во всех районах.
- Панель решений: слоты `N из 5`, мера, район, цена, «Убрать»; предварительные расходы, остаток или превышение, пометка, что итог считает сервер; «Возможная синергия: M10 + M12» только как справка, без чисел.
- «Оценить решения» оформлена через `aria-disabled` и остаётся фокусируемой. При недопустимом наборе нажатие ставит фокус на сводку «Что мешает оценке», а `onEvaluate` не вызывается. При `isEvaluating` весь конструктор блокируется через `<fieldset disabled>` и повторная отправка невозможна.
- `serverError` выводится отдельным блоком `role="alert"`, choices не очищаются.
- Live region сообщает счётчик, остаток и состояние отправки.
- Desktop: каталог 2/3 и закреплённая панель 1/3. На ширине до 767 px — одна колонка и фиксированная нижняя панель «N из 5 · остаток · Решения · Оценить» с safe-area. Панель решений открывается штатной кнопкой (`aria-expanded`/`aria-controls`) как нижний лист; Escape закрывает её и возвращает фокус. Контент получает нижний отступ под панель.
- Используются общие CSS variables из `styles/tokens.css` и `Button` из `components/ui`. Фильтры-чипы и текстовая «Убрать» оставлены своими: таких примитивов нет, второй UI kit не создавался.
- Score, лаги, районные баллы, реализованные эффекты и бонусы синергий не вычисляются.

## Проверки и фактический результат

Из `frontend/`, 23.09.2026:

| Проверка | Результат |
| --- | --- |
| `npm run lint` | exit 0, без замечаний |
| `npm run typecheck` (`tsc --noEmit`) | exit 0 |
| `npm test` (script GPT: `tsc -p tsconfig.test.json` + `node --test`) | 32 теста, 32 pass, 0 fail; из них 28 — `selection-rules.test.ts` |
| `next build` | **в общем `frontend/` не запускался**: там параллельно работал чужой dev-сервер на :3107, и я не стал пересобирать `.next`. Production build прошёл в изолированной копии (scratchpad: `features/planner` + `components/ui` + `styles` + `lib/contracts`): «Compiled successfully», TypeScript ок. |

Тесты (`selection-rules.test.ts`, fixture явно помечен как тестовый, цены условные):
- 5 мер из 4 направлений допустимы; 0 / 4 / 6 мер — `choice-count`;
- повтор M7 в разных районах — `duplicate-measure`;
- сумма ровно 100 допустима; 101 — `budget-exceeded` с «не хватает 1 ед.»; 100,004 — тоже ошибка, без округления;
- M7 + M8 + M9 — `category-limit`, две меры направления допустимы;
- без района, `""`, неизвестный район, неизвестная мера, city с `districtId`;
- `addChoice` / `setChoiceDistrict` не создают ключ `districtId` у city (проверка через `JSON.stringify`);
- M1 Нура + M3 Есиль — глобальный конфликт; M4 + M7 в Нуре — конфликт, в разных районах — нет; M5 + M13 в Сарыарке — конфликт, в Сарыарке и Алматы — нет;
- смена района создаёт и снимает конфликт; незавершённые меры не дают ложного конфликта;
- `rules: null` → только `rules-unavailable`;
- доступность: глобальный конфликт блокирует карточку; районный — только район; третья мера направления, бюджет, 5 из 5; удаление освобождает карточку; синергия только как пара.

## Проверка в браузере

`app/` менять нельзя, поэтому проверял через временную страницу в scratchpad, вне репозитория. На ней сценарий из `backend/.../ScenarioData.cs` с пометкой «Демонстрационные данные», родительский `useState` для choices и переключатели `isEvaluating`, `serverError`, `preferredDistrictId`, `rules: null`. После проверки сервер остановлен.

Проверено через DOM, на части шагов — скриншотами:
- контрольный набор M7 Нура, M8 Нура, M10 Нура, M12, M5 Сарыарка: 5 из 5, «95 из 100 ед.», остаток 5, «Возможная синергия: M10 + M12», `onEvaluate` вызван; payload `[{"measureId":"M7","districtId":"nura"},…,{"measureId":"M12"},…]`;
- M1 в Нуре → карточка M3 заблокирована с текстом причины; после удаления M1 M3 снова доступна;
- M4 в Нуре → у M7 опция «Нура — конфликт» disabled, остальные районы доступны, карточка доступна;
- M7 добавлена без района → «Для M7 не выбран район», нажатие «Оценить» фокусирует сводку, `onEvaluate` не вызван; назначение Есиля в слоте обновляет payload;
- `isEvaluating` → fieldset disabled, remove, select и «← К обзору» недоступны, кнопка показывает «Оцениваем решения…» со спиннером, повторный клик не вызывает `onEvaluate`;
- `serverError` → отдельный alert, choices сохранены;
- `rules: null` → предупреждение, заголовок «Каталог решений», оценка недоступна;
- `preferredDistrictId = nura` → район предложен в новых карточках; у M7 после добавления M4 в Нуру предложение сбрасывается на «не выбран»;
- 1440×900: сетка 2/3 + 1/3 (скриншот); 768×1024: две колонки (каталог 369 px, панель 320 px), переполнений нет; 390×844: одна колонка, нижняя панель 72 px, лист решений открывается и закрывается по Escape с возвратом фокуса, `scrollWidth` = 390, горизонтального скролла нет (скриншот).

## Что не проверено

- Интегрированный экран в `app/` с реальным C# API: API на :8080 не был запущен, fixture-переключателя в `Simulator` на момент проверки не нашёл. Сквозной сценарий 95 / 56,54 — за GPT.
- `npm run build` в общем `frontend/` (см. выше).
- Масштаб 200%, `prefers-reduced-motion` вживую и скринридер. Отключение переходов в CSS есть, но вручную не проверялось.
- Скриншоты при эмуляции размеров снимались нестабильно (таймауты, обрезка). Выводы по 768 и 390 сделаны в основном по измерениям DOM.
- Навыки `vercel-composition-patterns` и `react-best-practices` установлены только как SKILL.md, без файлов правил. Применены принципы из их quick reference: без boolean-режимов (подсказка района передаётся через `children`, доступность — discriminated union `status`), state поднят к родителю, derived state в render и `useMemo`, Map-индексы для lookup, явный `? :` вместо `&&`.

## Замечания и решения, о которых стоит знать

1. `serverError` показывается под заголовком «Ответ сервера» — одинаково для 400/422 и сетевых ошибок. Если GPT захочет разные заголовки, нужен отдельный prop; это изменение контракта, его нужно согласовать.
2. Районный конфликт через UI создать нельзя: опции с конфликтом disabled. `validateDraft` всё равно ловит такие choices, если они придут от родителя.
3. «Возможная синергия» показывается при наличии обеих мер пары, без учёта района. Это справка; фактическое применение — `appliedSynergies` из ответа.
4. Подписи направлений (`labels.ts`: «Транспорт», «Экология», «Социальная сфера», «Безопасность», «Городские сервисы») — UI-строки, не данные. Если у Claude 1 или в results другие формулировки, стоит договориться об общем словаре.
5. В `frontend/AGENTS.md` появился блок `<!-- BEGIN:nextjs-agent-rules -->`. Его дописывает `next dev`, скорее всего чужой сервер на :3107 в `frontend/`. Мой harness писал свой AGENTS.md в scratchpad. Сам файл я не трогал; решение за владельцем (архитектор).

## Блокеры и кому переданы

Блокеров нет. Для GPT: закоммитить `features/planner/` и этот отчёт. Файлы planner, `labels.ts` и тест уже включены в `tsconfig.test.json`. Прогнать `npm run build` и сквозной сценарий с реальным API.

## Изменения публичного контракта

Нет.
