# Отчёт GPT — редизайн и интеграция

## Commit

Общий commit этого этапа: `1e6082c feat(frontend): redesign routed simulator`.

## Реализовано

- App Router: `/`, `/decisions`, `/results`; URL определяет экран.
- Один `SimulationProvider` в корневом layout: выборы живут между переходами, черновик сохраняется в `sessionStorage` (`akim-draft-v1`) и проверяется по актуальному каталогу и правилам после загрузки сценария. Результат намеренно не переживает reload.
- Подключены модули Claude: `ThemeProvider`, ранний theme bootstrap, `ThemeSwitcher`, анимированный `AppHeader`, `Reveal`, `CityHome`, схема районов и обновлённый Planner.
- Три темы: system/light/dark/paper, ключ `akim-theme`. Локального легально полученного WOFF2 Manrope в репозитории нет, поэтому используется предусмотренный системный fallback; шрифт из сети не загружается.
- Results переработан под общие tokens: бюджет, Score и парные полосы «до/после» на шкале 0–100, районы, серверные синергии и explanation. `appliedEffects === null` не отображается.
- Сохраняются choices при API-ошибке, повторная отправка блокируется, устаревшие ответы игнорируются.

## Полировка 3D и достоверность (промежуточная фиксация)

- Добавлены `three@0.186.0` и `@types/three@0.186.0` (MIT) — единственная новая зависимость для изолированной сцены Claude 1, без R3F, Drei и дополнительных анимационных библиотек.
- `Explanation` теперь строго показывает `AI-анализ` для `llm` и `Шаблонное объяснение` для `mock`; текст от API не переписывается на клиенте.
- Добавлен авторский 16×16 favicon с условной городской башней: `/favicon.ico`; metadata явно ссылается на этот URL.
- Приняты исправления Claude 2 для узких экранов: явная колонка сетки, переносимые факты/мобильная строка и нескрываемый счётчик «5 из 5».
- На момент промежуточной фиксации `CityScene` от Claude 1 ещё не передан в общую копию. Зависимость готова, а SVG `AtlasSketch` остаётся действующим fallback; незаконченный canvas намеренно не подключён.

## Проверки

- `npm run lint` — успешно.
- `npm run typecheck` — успешно.
- `npm test` — 35/35.
- `npm run build` — успешно; маршруты `/`, `/decisions`, `/results` собраны.
- `git diff --check` — успешно.

## Browser / live API

Через `NEXT_PUBLIC_API_URL` из игнорируемого `frontend/.env.local` выполнен реальный flow в браузере:

`M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка` → расходы `95`, остаток `5`, Score `52,56 → 56,54`.

На результате видны применённая синергия `M10 + M12` и серверное AI-объяснение; фокус установлен на `h1` «Ваш план оценён».

Повторная проверка после промежуточного коммита: `/decisions` восстановил контрольный черновик после reload (`5 из 5`, остаток `5`); после reload `/results` показал предусмотренное пустое состояние, а переход обратно на `/decisions` сохранил черновик. Запрос `GET /favicon.ico` вернул `200 image/x-icon` (1118 bytes). Серверный результат для набора повторён: `95 / 5 / 52,56 → 56,54`; при `explanationSource=mock` интерфейс показал точную подпись «Шаблонное объяснение».

## Подключённые модули

- Claude 1: `components/ui/`, `features/city-overview/`, `styles/tokens.css`, `styles/globals.css`.
- Claude 2: `features/planner/`, включая регрессию stale district и mobile/desktop composition.
- GPT: `app/`, `features/simulator/`, `features/results/`, тест reducer и этот отчёт.

## Блокеры

Нет. Проверка экранов при 200% масштабе, forced-colors и скринридером вручную не выполнялась; CSS-обработчики этих режимов включены модулями Claude.

## Передача Кириллу — после релиза f3f1981

Новая frontend-версия будет **новее `f3f1981`**. В неё вошли уже интегрированные изменения Claude 1 (изолированный Three.js `CityScene` с SVG fallback) и Claude 2 (mobile planner), а также защита от двух синхронных нажатий «Оценить» до React rerender.

Фактически проверено локально через live API:

- Back/Forward между `/` и `/decisions`; путь `/results` появляется только после успешной оценки;
- reload `/decisions` восстановил набор M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка (`5 из 5`, остаток `5`);
- после оценки получены расходы `95`, остаток `5`, Score `52,56 → 56,54`; сервер отдал `AI-анализ`;
- удаление M5 сбросило результат: «Результат» стал недоступен, план изменился до `4 из 5`;
- прямая новая вкладка `/results` показывает честное пустое состояние;
- reducer-тесты проверяют сохранение choices после API-ошибки, разрешённый retry, stale response и второй `evaluate-start`; hook дополнительно закрывает синхронное окно `evaluationInFlight`;
- `GET /favicon.ico` — `200 image/x-icon`.

Для жюри добавлен `docs/demo.md`: двухминутный сценарий, контрольный набор и честный порядок действий, если API недоступен. `mock` в нём явно не называется AI-анализом.

## Локализация RU / KK / EN

- Созданы общий `LocaleProvider`/`useLocale` и тип `Locale` (`ru | kk | en`), ключ хранения `akim-locale`. Провайдер расположен выше ThemeProvider и SimulationProvider: смена языка и reload не очищают тему, choices или результат и не запускают POST.
- `html.lang` и заголовок вкладки синхронизируются с языком, включая восстановленный после reload язык. Форматтеры чисел принимают optional `intlLocale` (`ru-RU`, `kk-KZ`, `en-US`) без поломки старых вызовов.
- Центральный каталог переводит 5 районов, 14 мер, 10 показателей, 5 направлений и все 3 причины несовместимости по ID; неизвестные ID сохраняют API fallback.
- Подключены результаты, бюджет, Score, полосы до/после, районы, синергии, navigation и системные состояния. Подключены LanguageSwitcher и локализация UI/City/Planner от Claude 1 и Claude 2.
- Серверное explanation пока не содержит подтверждённого `explanationLocale`: для kk/en исходный текст не выдается за перевод, показана явная пометка о доступности объяснения на русском. `llm` — «AI-анализ»/локализованный эквивалент, `mock` — «Шаблонное объяснение»/локализованный эквивалент.

### Проверки локализации

- `npm test` — 44/44, включая полный каталог и инвариантность выбора/расходов при смене языка.
- `npm run lint`, `npm run typecheck`, `npm run build`, `git diff --check` — успешно.
- В браузере проверены kk и en, `html.lang`, title, навигация и сохранение kk после reload.

### Блокер/передача Тамерлану

Клиент **не добавляет** `Accept-Language` и не меняет тело POST, пока сервер не подтвердит контракт. Нужны: поддержка `Accept-Language: ru-RU | kk-KZ | en-US`, русский default, фактическое поле `explanationLocale: ru | kk | en`, локализованные mock/LLM explanation и стабильные error code. До этого server explanation честно обозначается как русскоязычный fallback.

### Финальное исправление после localization review

- Замечание P2 исправлено: `createLocaleStore` держит выбранный язык в памяти текущей вкладки, если `localStorage` запрещён, заполнен или выбрасывает исключение. При доступном storage он по-прежнему является источником для reload и события `storage` между вкладками.
- Добавлены регрессии blocked-storage и normal-storage. Финальный прогон: `npm test` 46/46, `npm run lint`, `npm run typecheck`, `npm run build`, `git diff --check` — успешно.

### Предыдущая локальная реализация языкового контракта (заменена при интеграции)

- Клиент отправляет `Accept-Language` (`ru-RU`, `kk-KZ`, `en-US`) с запросами сценария и оценки, не меняя JSON body `{ choices }`.
- Адаптер читает `explanationLocale` и `Content-Language`, нормализует BCP 47 tags и отклоняет противоречивый ответ. Результат хранит фактический язык explanation; при переключении UI прежний текст не переименовывается и не запускается новый POST.
- Реальная проверка control set на опубликованном API: для ru-RU/kk-KZ/en-US совпали `Content-Language` и `explanationLocale`; получены русское, казахское и английское explanations, при неизменных `spent=95`, `remaining=5`, `Score 52.56 → 56.54`.
- Browser: English result показал `AI analysis` и `Explanation language: English`; после перехода UI на kk уже полученный текст остался явно помечен как английский.
## Подключение языка explanation к backend (23.09.2026)

Backend поддерживает `Accept-Language: ru-RU / kk-KZ / en-US` и возвращает фактический `explanationLocale`. Клиент передаёт `intlLocale` из общего провайдера, тело POST остаётся `{ choices }`. Адаптер сохраняет локаль; отсутствие поля у старого API трактуется как русский, неизвестное значение отклоняется.

После смены языка результата старое объяснение явно помечено своим языком и размечено `lang`. Переключение не отправляет POST. Явная повторная оценка сохраняет решения и показывает предыдущий отчёт во время ожидания.

При интеграционном review исправлена потеря отчёта после ошибки повторной оценки: предыдущий результат сохраняется только при совпадающей revision; показаны локализованное сообщение и повтор. Reset/изменение выбора удаляют устаревший отчёт.

Проверки координатора: typecheck, lint, 50/50 тестов; production Docker build прошёл, frontend пересоздан и healthy. Основной API и его ключ не изменялись. Браузерный прогон с временным mock API завершён: 24 проверки прошли. RU → смена EN без POST → сетевой отказ с сохранением отчёта/решений → успешный EN retry → KK. Везде 95 / 56.54; при ширине 390 px переполнения нет. Настоящих AI-запросов не было. Подробности и снимки — в корневом docs/reports/e2e.md.

### Release sync и локализация ошибок

- Общая копия синхронизирована с `origin/main`; изменения Claude 1 в `components/ui` и `features/city-overview` включены в релиз без reset/stash.
- Временный `Explanation.tsx` сравнен с опубликованной BCP 47-реализацией: сохранены `lang`, определение устаревшего explanation и явный re-evaluate; добавлена только безопасная текстовая отрисовка неожиданного объекта в списке.
- Reducer хранит `kind`/`code`/`detail` ошибки, а UI форматирует её при каждом рендере по текущему RU/KK/EN locale. Известные стабильные API-коды локализованы; неизвестный код оставляет server detail после общего переведённого сообщения.
- Финальные проверки: `npm run lint`, `npm run typecheck`, `npm test` 55/55 и `npm run build` — успешно. `localhost:3000/results` отвечает 200 после перезапуска стенда.

### Исправление финальной приёмки Claude 2

- Для неизвестных HTTP-кодов и ошибок контракта русская `detail` больше не присоединяется к пользовательской строке. Пользователь видит только локализованное основное сообщение; техническую деталь UI не показывает, так как она не требуется для восстановления сценария.
- Ошибка хранит стабильные `kind`/`code` и форматируется во время рендера: уже показанная ошибка меняет текст сразу при переходе RU/KK/EN.
- Регрессия проверяет unknown HTTP `500` и contract response на KK и EN, исключая русский detail. Повторно успешны lint, 55/55 тестов и production build.
